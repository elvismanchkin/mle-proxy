<configuration>
    <appender name="TEST-CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="TEST-CONSOLE"/>
    </root>

    <!-- Set lower log levels for noisy classes -->
    <logger name="io.micronaut" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="com.rabbitmq" level="WARN"/>
</configuration>